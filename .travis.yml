sudo: true
os: ubuntu
#haha chris was here!!!!
ubuntu: 
        - 18.04
env:
        - FLASK_CONFIG=staging 
language: python
python:
        - "3.7.2"
          #        - "10"
before_install:
        - sudo apt-get update
          #matrix:
          #        include:
          #      - language: python
          #        python:
          #             - "3.7.2"
          #        before_install:
        - yarn cache clean
        - yarn install
script:
        - echo "Hello World"
deploy:
provider: linode
local-dir: public
branchs:
        only: 
                - development
