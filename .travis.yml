sudo: true
os: ubuntu
#haha chris was here!!!!
ubuntu: 
        - 18.04
env:
        - FLASK_CONFIG=staging 
before_install:
        #        - python --versdfddfsdon
        - sudo apt-get update
        - python --version
          #  igggg      - sudo apt-get install python3
          #        - sudo apt-get upgrade python3
matrix:
        include:
#               - language: node_js
#                 node_js: "11"
#                 before_install:
#                         - yarn cache clean
#                 install:
#                         - yarn install
#                 script:
#                         - echo "Hello chris"      
                                 #yarn cache clean
                                  #- yarn install
                                  # -dfdfsdfsddf lajkdfkdjfnguage: python
                - language: python
                  python: "3.7"
                  dist: "xenial"
                  sudo: true
                  before_install:
                        - which corpus_christi
                        - python -m venv venv
                  script:
                        - which pip
                        - source venv/bin/activate
                        - pip install -r requirements.txt
                        - echo "Hello World"
                  after_script:
                        - deactivate
deploy:
provider: linode
local-dir: public
branchs:
        only: 
                - development
