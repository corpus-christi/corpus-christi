module: roles
entities:
  - name: Role
    singular: role
    plural: roles
    uri: /roles
    table: roles_role
    attributes:
      - name: id
        type: integer
        primary-key: true
        min: 1
      - name: name_i18n
        type: string
        length: locale
